
<div style="margin-top: -40px; width: 100%; height: 50px; line-height: 40px; color: red; text-align: right;">
    <%= link_to 'Back', '/sessions/sellersaccount', :class => 'submit-btn links' %>
</div>
<div>
    <h3 style="font-size: 20px; color: #5E6469;">Product Order History</h3>
    <br>
    <hr/>
    <br>
    <table style="margin-bottom: 100px;">
        <thead>
            <tr>
            <th>OrderId</th>
            <th>Customer</th>
            <th>Product</th>
            <th>Quantity</th>
            <th>City</th>
            <th>Date</th>
            </tr>
        </thead>

        <tbody>
            <% @order_products.each do |order_product| %>
            <tr>
                <td><%= order_product.order_id %></td>
                <td><%= order_product.order.customer.name %></td>
                <td><%= order_product.product.name%> </td>
                <td><%= order_product.quantity %></td>
                <td><%= order_product.order.address.city %> </td>
                <td>₹ <%= order_product.order.order_date %></td>
                </td>
            </tr>
            
            <% end %>
        </tbody>
    </table>
</div>